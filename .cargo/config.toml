# Cargo 配置文件
# 用于设置构建时的环境变量

[env]
# 自动设置 PKG_CONFIG_PATH 以找到 FFmpeg 库
# 注意：这里使用脚本动态获取路径
# 如果脚本不存在或失败，可以手动设置：
# PKG_CONFIG_PATH = "/opt/homebrew/Cellar/ffmpeg@7/7.1.3/lib/pkgconfig:/opt/homebrew/lib/pkgconfig"

# 由于 TOML 不支持动态值，建议在 shell 中设置环境变量
# 或者在 package.json 的脚本中设置

