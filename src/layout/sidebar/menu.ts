export enum MenuItems {
  home = '/',
  aiTools = '',
  myFiles = '/my/files',

  converter = '/converter',
  compressor = '/compressor',
  
  metadata = '/metadata',
  
  
  tasks = '/tasks',
  batch = '/batch',
  audio = '/audio',
  video = '/video',
  image = '/image',
}